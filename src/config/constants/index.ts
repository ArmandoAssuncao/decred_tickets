import { IConstants } from './types'

const constants: IConstants = {
    APP_NAME: 'DecredTickets',
    HTTP_HEADERS: {
        ACCEPT: 'application/json',
    },
    REDUX_PERSIST_KEY: 'redux_persist_key',
}

export default constants
